# Решение проблем с импортом токенов в Figma

## ✅ Проверка формата файла

Убедитесь, что файл `figma-tokens.json` имеет правильный формат. Откройте файл и проверьте, что структура выглядит так:

```json
{
  "color": {
    "dark": {
      "bg-primary": {
        "type": "color",
        "value": "#0f0f0f"
      }
    }
  }
}
```

**Важно:** Должно быть `"color"` (не `"colors"`) и каждый токен должен иметь поле `"type"`.

## 🔧 Как правильно импортировать в Tokens Studio

### Способ 1: Через Settings (Рекомендуется)

1. Откройте плагин **"Tokens Studio for Figma"**
2. Перейдите в `Settings` (шестеренка внизу)
3. Нажмите `Token sources`
4. Нажмите `+ Add source`
5. Выберите `JSON`
6. Выберите файл `figma-tokens.json`
7. Нажмите `Save`

### Способ 2: Через Import

1. Откройте плагин **"Tokens Studio for Figma"**
2. Нажмите `Import tokens`
3. Выберите `JSON`
4. Выберите файл `figma-tokens.json`
5. Токены должны появиться в списке

### Способ 3: Если ничего не работает

Если плагин все равно не видит токены, попробуйте:

1. **Проверьте версию плагина:**
   - Убедитесь, что используете последнюю версию "Tokens Studio for Figma"
   - Обновите плагин, если нужно

2. **Используйте альтернативный формат:**
   - Некоторые версии плагина требуют плоскую структуру без вложенных тем
   - Попробуйте создать файл вручную с плоской структурой

3. **Ручной импорт цветов:**
   - Откройте `figma-tokens.json`
   - Скопируйте значения цветов
   - В Figma: `Design` → `Color styles` → `+` → создайте стили вручную

## 📋 Структура токенов в файле

Текущий файл содержит:

- **color.dark** - цвета для темной темы (15 токенов)
- **color.light** - цвета для светлой темы (15 токенов)
- **spacing** - отступы и радиусы (5 токенов)
- **typography** - типографика (2 токена: h2, body)
- **shadow** - тени и эффекты (4 токена)

## 🐛 Частые проблемы

### Проблема: "Файл пустой" или "Не найдено токенов"

**Решение:**
- Убедитесь, что файл сохранен в кодировке UTF-8
- Проверьте, что JSON валидный (можно проверить на jsonlint.com)
- Попробуйте пересоздать файл: `npm run export:figma`

### Проблема: "Неправильный формат"

**Решение:**
- Убедитесь, что используете плагин **"Tokens Studio for Figma"** (не просто "Figma Tokens")
- Проверьте, что в файле есть поля `type` для каждого токена
- Попробуйте использовать способ через Settings вместо Import

### Проблема: "Токены импортированы, но не видны"

**Решение:**
- В плагине переключите вкладку на нужную категорию (Colors, Spacing и т.д.)
- Проверьте фильтры в плагине
- Попробуйте перезапустить плагин

## 💡 Альтернативные решения

Если Tokens Studio не работает, можно:

1. **Использовать плагин "Design Tokens":**
   - Установите плагин "Design Tokens"
   - Он может работать с другим форматом

2. **Использовать html.to.design:**
   - Импортируйте всю страницу через плагин html.to.design
   - Это создаст визуальные элементы, но не токены

3. **Ручное создание стилей:**
   - Используйте значения из `figma-tokens.json` как референс
   - Создайте Color Styles, Text Styles вручную в Figma

## 📞 Дополнительная помощь

- [Документация Tokens Studio](https://docs.tokens.studio/)
- [Figma Community Forum](https://forum.figma.com/)
- [Tokens Studio GitHub](https://github.com/tokens-studio/figma-plugin)

