# Руководство по импорту дизайнов в Figma

Это руководство описывает несколько способов переноса ваших HTML/CSS страниц в Figma.

## 🎯 Способ 1: Плагин "html.to.design" (Рекомендуется)

Это самый простой и точный способ импорта HTML страниц в Figma.

### Шаги:

1. **Установите плагин в Figma:**
   - Откройте Figma
   - Перейдите в меню: `Plugins` → `Browse plugins in Community`
   - Найдите и установите плагин **"html.to.design"**

2. **Запустите локальный сервер:**
   ```bash
   npm run dev
   ```

3. **Откройте страницу в браузере:**
   - Откройте нужную страницу (например, `http://localhost:3000/task17`)
   - Убедитесь, что страница отображается корректно

4. **Используйте плагин:**
   - В Figma: `Plugins` → `html.to.design` → `Import from URL`
   - Вставьте URL вашей страницы
   - Плагин автоматически создаст фреймы с элементами

### Альтернатива: Браузерное расширение

1. Установите расширение **"html.to.design"** для Chrome/Edge
2. Откройте вашу страницу в браузере
3. Нажмите на иконку расширения
4. Выберите элементы для экспорта
5. Экспортируйте в Figma

---

## 🎨 Способ 2: Импорт дизайн-токенов (Цвета, типографика, отступы)

Этот способ позволяет импортировать CSS переменные (цвета, шрифты, отступы) в Figma для использования в дизайн-системе.

### Шаги:

1. **Экспортируйте токены:**
   ```bash
   node scripts/export-to-figma.js
   ```
   
   Это создаст файл `figma-tokens.json` с вашими CSS переменными.

2. **Установите плагин Figma Tokens:**
   - В Figma: `Plugins` → `Browse plugins` → найдите **"Figma Tokens"**
   - Установите плагин

3. **Импортируйте токены:**
   - Откройте плагин: `Plugins` → `Tokens Studio for Figma` → `Import tokens`
   - Выберите опцию `JSON` или `Import from file`
   - Выберите файл `figma-tokens.json`
   - **Важно:** Убедитесь, что файл имеет структуру с полями `type` и `value` (это уже настроено в скрипте)
   - Токены будут доступны в Figma для использования

### ⚠️ Если плагин не видит токены:

1. **Проверьте формат файла:**
   - Файл должен содержать поля `type` для каждого токена
   - Например: `"type": "color"`, `"type": "dimension"`, `"type": "typography"`
   - Текущий скрипт уже создает правильный формат

2. **Попробуйте другой способ импорта:**
   - В плагине Tokens Studio: `Settings` → `Token sources` → `Add source` → `JSON`
   - Или используйте опцию `Sync` вместо `Import`

3. **Альтернатива - ручной импорт цветов:**
   - Если автоматический импорт не работает, можно создать Color Styles вручную
   - Используйте значения из `figma-tokens.json` как референс

### Что будет импортировано:
- ✅ Цвета (dark/light темы)
- ✅ Отступы (spacing)
- ✅ Типографика (размеры шрифтов, line-height)
- ✅ Эффекты (тени, свечение)
- ✅ Радиусы скругления

---

## 📸 Способ 3: Скриншоты + ручное воссоздание

Если автоматические инструменты не подходят, можно использовать скриншоты как референс.

### Шаги:

1. **Сделайте скриншоты страниц:**
   - Откройте страницу в браузере
   - Используйте инструменты разработчика для скриншотов элементов
   - Или используйте расширение для полностраничных скриншотов

2. **Импортируйте в Figma:**
   - Перетащите скриншоты в Figma
   - Используйте их как референс для воссоздания дизайна

3. **Воссоздайте дизайн:**
   - Используйте импортированные токены (из Способа 2) для точного соответствия цветам и размерам
   - Воссоздайте компоненты вручную

---

## 🔧 Способ 4: Использование CSS переменных напрямую

Если вы хотите использовать CSS переменные в Figma напрямую:

1. **Скопируйте CSS переменные:**
   - Откройте `public/styles.css`
   - Найдите секцию `:root` с переменными
   - Скопируйте нужные значения

2. **Создайте стили в Figma:**
   - Создайте Color Styles для цветов
   - Создайте Text Styles для типографики
   - Используйте значения из CSS переменных

### Пример маппинга CSS → Figma:

| CSS переменная | Figma Style |
|---------------|-------------|
| `--bg-primary` | Color Style: `Background/Primary` |
| `--text-primary` | Color Style: `Text/Primary` |
| `--neon-cyan` | Color Style: `Accent/Cyan` |
| `--h2` | Text Style: `Heading/H2` |
| `--space-16` | Spacing: `16px` |

---

## 📋 Рекомендуемый workflow

1. **Для быстрого импорта всей страницы:**
   - Используйте Способ 1 (html.to.design)

2. **Для создания дизайн-системы:**
   - Используйте Способ 2 (Figma Tokens) для импорта переменных
   - Создайте компоненты в Figma на основе импортированных токенов

3. **Для точного соответствия:**
   - Комбинируйте Способ 1 и Способ 2
   - Импортируйте структуру через html.to.design
   - Примените токены через Figma Tokens

---

## 🚀 Быстрый старт

```bash
# 1. Экспортируйте токены
node scripts/export-to-figma.js

# 2. Запустите сервер
npm run dev

# 3. Откройте страницу в браузере
# 4. Используйте плагин html.to.design в Figma
```

---

## 📚 Полезные ссылки

- [html.to.design плагин](https://www.figma.com/community/plugin/1159123024924461424)
- [Figma Tokens плагин](https://www.figma.com/community/plugin/888356646278934516)
- [Figma Tokens документация](https://docs.tokens.studio/)

---

## ⚠️ Ограничения

- Плагины могут не идеально передавать сложные CSS эффекты (blur, gradients)
- Интерактивные элементы (JavaScript) не будут работать в Figma
- Некоторые CSS свойства могут требовать ручной доработки

---

## 💡 Советы

1. **Используйте темную и светлую темы отдельно:**
   - Импортируйте каждую тему как отдельный вариант
   - Используйте Figma Variants для переключения тем

2. **Организуйте структуру:**
   - Создайте отдельные страницы для каждой темы
   - Группируйте компоненты по функциональности

3. **Сохраняйте токены:**
   - Регулярно обновляйте `figma-tokens.json` при изменении CSS
   - Используйте версионирование для отслеживания изменений

